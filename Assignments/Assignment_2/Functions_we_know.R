
readLines()
list.files()
read.csv()
c()
head()
length()
for (variable in vector) {
  
}

plot()

plot(x=df$mass,y=df$wingspan)
#this makes a plot

sum(df$wingspan)

mean(df$wingspan)

median(df$wingspan)

sd(df$wingspan)

min(df$wingspan)

max(df$wingspan)

summary(df$wingspan)

#anything in quotes is not seen as a numerical number

library(tidyverse)
#find it in google

library(tidyverse)
df %>% 
  ggplot(aes(x=mass,y=wingspan,variety=color))
geom_point() + geom_smooth(color=black)
#this sets up a table with the information with the adjustments stated

